name: organizagrana
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  go_router: ^17.0.0
  flutter_web_plugins:
    sdk: flutter
  web: ^1.1.1
  http: ^1.5.0
  shared_preferences: ^2.5.3
  material_table_view: ^5.5.2
  excel: ^4.0.6
  syncfusion_flutter_charts: ^28.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/config/nav_menu.json
